#include <stdio.h>
#include <stdlib.h>
/**
 * main - A program that prints numbers for 0 to 9
 * Return: Always (Success)
 */

int main(void)
{
int i = 0;
while (i < 10)
{
printf(i);
i++;
printf('\n');
return (0);
}
}
