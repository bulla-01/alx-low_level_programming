#include "main.h"
/**
 * mul - Multiply two integers
 * @x: The first number
 * @y: The second number
 * Return: The results of multiplication between two numbers
 */
int mul(int x, int y)
{
return (x * y);
}
